packages:
  - 'apps/*'
  - 'packages/*'
  - 'functions'
allowBuilds:
  # Firebase web SDK build scripts aren't needed in a bundler (Vite/WXT) — skip them.
  '@firebase/util': false
  esbuild: true
  protobufjs: false
  spawn-sync: true
