// Cloud Functions (poller, notification triggers, billing kill-switch).
// Phase 0 step 4 records cost-safety constants in ./limits.ts (no deploy).
// Phase 3 adds the poller, the budget kill-switch, and Blaze.
export {};
